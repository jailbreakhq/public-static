a.th.radius(href="/teams/#{slug}")
  img(src="#{avatar}", width=150, height=150)
h3
  a(href="/teams/#{slug}")
    | #{names}
    if featured
      span.featured &sstarf;

div
  span.university #{university}
  span.amount-raised &euro;#{(amountRaisedOnline+amountRaisedOffline)/100}
